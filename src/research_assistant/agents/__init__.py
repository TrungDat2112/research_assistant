"""Agent roles (planner, synthesizer, critic, reporter).

Each agent is a pure function over ``ResearchState`` used as a node in
the LangGraph pipeline defined in :mod:`research_assistant.graph`.
"""
